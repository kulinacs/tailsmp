---
- name: Updating software
  apt:
    update_cache: yes
    upgrade: "yes"
- name: Adding group ssh
  group:
    name=ssh
    state=present
- name: Adding user
  user:
    name={{ setup_user }}
    groups=sudo,ssh
    append=yes
    password={{ setup_pass }}
